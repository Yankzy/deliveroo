import { View, Text, SafeAreaView } from 'react-native';


const About = () =>{
    return (
      <SafeAreaView className="mt-10">
        <Text className="text-red-500">About Screen</Text>
      </SafeAreaView>
    );
  }

export default About;